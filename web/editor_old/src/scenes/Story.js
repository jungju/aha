import Phaser from 'phaser'

export default class extends Phaser.Scene {
  constructor () {
    super({ key: 'StoryScene' })
  }

  preload () {
    //TODO: storybook 가져오기
    
  }

  create () {
    
  }

  update () {

  }
}
